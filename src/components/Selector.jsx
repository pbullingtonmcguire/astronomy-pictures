const Selector = () => {

    return (
        <div class='selector'>
            <button>{'<'}</button>
            <input type="date" />
            <button>{'>'}</button>
        </div>
    )
}

export default Selector